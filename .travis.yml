before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version 1.7.0
  - export PATH="$HOME/.yarn/bin:$PATH"

language: node_js
node_js:
   - "10.5"
cache: yarn

language: rust
rust:
  - nightly

notifications:
  email: false
